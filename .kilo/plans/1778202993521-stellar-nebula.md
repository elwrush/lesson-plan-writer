# Plan: Update slideshow with Pixabay image

## Goal
Add a downloaded Pixabay image to the existing slideshow at `output/substitute-lesson-1-M3/site/index.html`.

## Missing Information
Need to clarify:
1. **File location of downloaded image** — What is the filename and folder path of the Pixabay image you downloaded? (e.g., `output/substitute-lesson-1-M3/images/image-name.jpg`)
2. **Placement** — Which slide/section should the image appear in? The slide content includes:
   - Slide 1: Title slide ("What Connects Us")
   - Lead-in slide (has a placeholder for a photo from materials)
   - Multiple reading stages
   - Answer key slides
   - Thank you slide

Please provide:
- The image file path
- The slide number or title where you want the image inserted

## Next Steps (for implementation, after clarification)
1. Read the current `index.html` to understand structure
2. Place image in appropriate location with proper reveal.js syntax:
   ```
   ![Caption](image-path)
   ```
   or HTML `<img>` tag within the slide markdown
3. Ensure proper sizing via CSS (max-height: 400px already defined)